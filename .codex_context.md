Context (request 2025-XX-XX)
-----------------------------
- Topic: pprz/tunnel UAVCAN messages currently carry an XBee-specific header. Paparazzi header already has source/dest/class; XBee header mainly provides link metadata (e.g., RSSI) and mesh-related info. Team feedback pending on whether to keep/drop/genericize this header.
- Options considered:
  * Strip XBee-specific fields, keep a generic link block (e.g., RSSI, LQI/SNR, flags/hop count) or a minimal optional RSSI field.
  * If backward compatibility needed, add generic tag and deprecate XBee tag.
- Pending action: ask team whether mesh/path data from XBee header matters; adjust DSDL and tunnel code accordingly.

IMU/DroneCAN role idea (future work)
------------------------------------
- Goal: add a high-rate SPI InvenSense IMU role publishing over DroneCAN (e.g., RawIMU + optional vibration metrics), filtered with RPM-driven notches using DSHOT telemetry received on CAN.
- Use DSHOT telemetry RPM to set notch center freq (with harmonics) and cross-fade filter coeffs; keep filters in the IMU role so it can be evaluated independently of Paparazzi’s estimator.
- Run in parallel initially for logging/validation without affecting control; add params for rate, notch Q/harmonics, min/max freq, vibration metrics logging.
